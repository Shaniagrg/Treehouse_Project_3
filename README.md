<h1>Real-Time Error Messages</h1>

<p>The name field listens to the 'keyup' as user types. Everytime the user type in their name it will prompt a message saying its correct. If left blank it promts a message that asks you to fill the input field</p>

<h1>Conditional Error Message</h1>
<p>If the user leave it blank or the format of the email isn't correct then we want to make sure it's correctly formatted. We prompt message if any error in the input filled</p>